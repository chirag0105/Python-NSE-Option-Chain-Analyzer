# Architecture

> Auto-generated by /map on 2026-02-24

## Overview

Python NSE Option Chain Analyzer is a desktop application built with Tkinter that fetches, analyzes, and displays live options chain data from the National Stock Exchange of India (NSE). It provides filtering for stocks vs. indexes and automatically recalculates critical values like Call/Put Open Interest (OI) changes, implied volatility, and volume, continuously refreshing based on user-defined intervals. It also allows exporting data to CSV.

\\\
┌─────────────────────────────────────────┐
│        NSE_Option_Chain_Analyzer.py     │
│        (Main Entry & UI App Class)      │
├─────────────────────────────────────────┤
│        [Data Fetching & Parsing]        
          api requests, pandas           

            [UI Rendering]               
          tkinter, tksheet               

\\\

## Components

### NSE_Option_Chain_Analyzer.py (Nse Class)
- **Purpose:** Core monolithic application handling UI structure, event loop, background jobs, NSE data fetching, data processing using Pandas, and saving/loading configurations.
- **Location:** \/NSE_Option_Chain_Analyzer.py\
- **Dependencies:** \equests\, \pandas\, \	ksheet\, \	kinter\, \configparser\

## Data Flow

1. Initialization: The \Nse\ class sets up the main loop and loads configurations.
2. Login/Selection: User selects the underlying asset (Index/Stock) and the expiration date via dropdowns.
3. Data Fetching: It requests the option chain JSON data from NSE India API urls.
4. Parsing & Processing: The raw JSON is loaded into a Pandas DataFrame, sorted, filtered, and aggregated (e.g., finding highest/second-highest OI).
5. Presentation: Processed data is fed into \	ksheet\ components to render the full option chain grid.
6. Refresh Loop: A background timer periodically checks for updates and re-fetches Data Flow step 3, replacing the data grid without fully destroying the window. Data can also be automatically saved to CSV if enabled.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| NSE India API | HTTP/REST | Fetch live option chain data (e.g. \https://www.nseindia.com/api/option-chain-indices\, \https://www.nseindia.com/api/option-chain-equities\) |

## Technical Debt

- [ ] Monolithic structure. The \Nse\ class does too much (UI management, data fetching, processing). Consider splitting into separation of concerns (e.g., separate \DataFetcher\, \OptionChainProcessor\ modules).
- [ ] No explicit test files found. The \pytest\ or \unittest\ modules are not utilized. 
- [ ] Uses direct \	ime.sleep\ intertwined with GUI updates which can cause small UI lag depending on the implementation.

## Conventions

**Naming:** PascalCase for class (\Nse\), snake_case for methods (\get_dataframe\, \get_symbols\).
**Structure:** Single-file monolithic application structure.
**Testing:** No automated testing framework structure found.
